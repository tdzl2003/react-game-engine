/**
 * Created by tdzl2003 on 03/06/2017.
 */

export default class BaseGLNodeManager {
  constructor(bridge) {
    this.bridge = bridge;
  }

  setViewTag(view, tag) {
  }

  setChildren(view, children) {
  }

  setViewProps(view, props) {
  }
}

